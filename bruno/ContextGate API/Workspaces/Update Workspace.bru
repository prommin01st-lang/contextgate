meta {
  name: Update Workspace
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/api/workspaces/{{workspaceId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Updated Workspace",
    "settings": {
      "theme": "dark"
    }
  }
}
