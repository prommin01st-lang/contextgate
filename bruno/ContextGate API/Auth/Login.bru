meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "admin@contextgate.local",
    "password": "password123"
  }
}

vars:post-response {
  token: res.body.token
}
